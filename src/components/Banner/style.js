import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    imageBanner:{
        width: "90%",
        height: "20%",
        marginTop: 30,
        marginBottom: 30,
        borderRadius: 10,
        gap: 10,
      },
      textBanner: {
        color: 'white',
        width: '90%',
        fontSize: 25,
        marginTop: 30,
        fontWeight:'bold',
      }
})

export default styles;